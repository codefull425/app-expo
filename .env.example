# Opcional: só origem (host:porta). Não inclua /categories aqui — isso duplicava o path e dava 404.
# EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:3000

# Path (default: /categories). Não use /items se algum cliente acrescentar ?_=… (rewrite json-server → 404).
# EXPO_PUBLIC_API_CATALOG_PATH=/categories

# API mock: na pasta cantina-expo → npm run api
